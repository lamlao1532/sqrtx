class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        arr = []
        for i in nums:
            for j in nums:
                if(nums[i] + nums[j] == target and i != j):
                    arr.append(i)
                    arr.append(j)
        return arr
